# Current Task

@guide.md

## Request

继续把 `LoreWeaver/data/workspaces/20260611-060754-719406` 建设成一款真正成熟的移动端 H5 游戏。此前以 `/Users/lm/pyProj/doc/GameLib/minigame/Path_to_Immortality` 和 `/Users/lm/pyProj/doc/GameLib/minigame/three_kingdoms_brawl` 的约 8/10 成熟度为参照，但当前成果与最终 9/10 目标仍有明显差距。

要求先进行深入、批判性的差距复核，再输出可逐步落地、可验证、可持续执行的任务计划，并继续实现直至目标完成。遇到普通产品、技术、架构和实现取舍时由 Agent 自主决策；只有涉及不可逆存档迁移、公开 IP 方向、付费或受限资产授权、外部账号/密钥、发布策略等确实需要人类参与的事项才提出协作请求。

编码实现优先使用 `gpt-5.6-terra` 执行角色，Codex 负责方案、任务拆解、证据复核和最终 review。

## Acceptance Criteria

- 建立独立于“文件数量、文档完整、manifest 合法、场景能启动”的 100 分游戏成熟度评分卡；最终总分至少 90/100，且所有硬性封顶条件解除。
- 核心战斗不能只剩移动和自动施法。玩家在移动端必须拥有可理解、可练习、可形成取舍的主动操作，敌人必须有可读行为、反制窗口和受击反馈。
- 数值在 Node1-12 全程保持可玩：不得出现普通技能秒杀最终 Boss、后期玩家近乎无敌、奖励成本脱节或失败没有学习价值的情况。
- Node1-12 都必须成为完整关卡，而不是机制标签。每关都有节奏分段、目标、敌人组合、高潮/Boss、失败原因、奖励落点和复玩指标；Node12 必须是完整多阶段终局战。
- 成长系统必须形成至少 3 条可辨认构筑路线，并包含局内质变、局外选择、首通奖励幂等、最佳成绩/星级、可追求掉落或挑战目标。
- 720x1280 与常见窄屏手机可舒适游玩；HUD 不遮挡战场，触控区域符合人体工学，文字和数值可读，暂停、重试、升级选择、主动技能和返回流程完整。
- 生产视觉以真实 bitmap 资产和动画为主。主角、常规敌人、精英、Boss、关键技能、道具和场景都具备语义化 art key、动作帧与可验证覆盖；程序化图形只作低调兜底。
- 建立真实音频管线：关卡/Boss BGM、战斗 SFX、UI/胜负提示、必要的招式喊名或视觉 callout 均通过 manifest、音频通道和用户控制接入运行时。
- 从零存档可自然完成关键主线，不依赖测试注入；自动化覆盖自然进度、Node1-12 冒烟、每关核心机制、Boss 阶段、移动端输入、视觉布局、性能、存档迁移和导出包启动。
- 公开分享前，活跃运行时、导出产物和玩家可见内容必须通过原创性/IP 清理；历史文档不影响运行时验收，但不能泄露到公开包。
- LoreWeaver 不只生成 manifest 预览。它最终必须能生成/维护完整 workspace 源码、资产 manifest、验证脚本和独立可玩导出包，并能把本项目的高质量模板复用于新项目。

## Notes

- 任何 gate 只能证明它实际断言的事实。`build passed`、`12/12 scenes entered`、`no overlap`、`atlas loaded` 均不得被写成“游戏成熟”或“体验通过”。
- 优先修复战斗基座、数值曲线和玩家决策，再扩写 12 关内容；否则新增资产和关卡会建立在错误底座上。
- 先做达到 9/10 的 Node1 纵向切片，再把同一质量合同扩展到 Node2-12；不接受平均铺开后每关仍然空薄。
- 每个任务必须声明玩家价值、依赖、目标文件、patch level、done criteria、required gates、可复查证据和残余风险。
- 最终验收以实玩路径、视觉证据、音频证据、性能数据、自然存档流程和可重复导出为准，不以文档篇幅或代码行数为准。
