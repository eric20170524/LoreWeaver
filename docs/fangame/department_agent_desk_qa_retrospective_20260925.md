# 质检复测与「制作筹备 · 部门 Agent 台」复盘（2026-09-25）

## 口径

当前精确候选包是 `productize/exports/standalone-xuanjie-shimu-local-20260924211606.zip`，SHA-256 `98424174998a3fa35457a5b36a2f39ab7616120d526672c731a6b02418156779`。以下“已修复”只表示指定缺陷在此包的对应自动测试和本地画面范围内复测通过；不等于真人、实体设备或独立视觉模型验收。部门 `confirmed` 表示筹备内容已确认，不表示发布已认证。

## 质检发现、复测结果

| 类别 | 发现的问题 | 当前结果与边界 |
| --- | --- | --- |
| 主干与玩法 | 早期场景切换触发已销毁 HUD/Group 引用；竞技波次曾把限时改成 99999 秒，第一波敌人刷在屏外；节点 3/6/11 长局和节点 9 守线曾过难或来敌出生在防线错误一侧。 | 修正后主干进出关、原卡胜负/撤退、节点 1/8/12 清波及超时、节点 3/6/11 多种子长局、节点 9 全 120 秒守线均有精确包自动证据。理想脚本路线仍不代表真人难度。见 `docs/0_TASKLIST.md` Task 16、19、20。 |
| 横屏与输入 | 节点 4 最初以竖屏窄带显示、缺四向/轻重击六键，俯视底图不符合横版；横版敌人远小于主角。 | 当前包为 844×390 横屏，竖握提示并暂停计时/BGM，三段侧视背景与三波清场可见；15 项横屏专项通过，普通敌人/Boss 显示宽 210/294。`standalone_brawler_orientation_20260924211606.json` 绑定当前 ZIP。真机触控手感未验。 |
| 美术与可读性 | 12 关背景曾降级成程序画面；手机 HUD/触控与画布拥挤；节点 5 持弓角色小且动作近似同帧，攻击帧曾有跨格碎片；节点 6 毒圈遮人、节点 7 绿色环抢月华色、节点 8 第五波精英/Boss 退化为粉色几何体，节点 8/9 敌人偏小。 | 环境与角色图集、布局、层级和敌人比例已逐项修正。当前包桌面/手机模拟视口各 12/12，节点 8/9 的显示放大保留原碰撞体；本地截图已复核。节点 2/4/5 独立 VLM 和长局动态画面审计仍开放。 |
| 音频 | 初期仅有不同频率底噪；节点 10 心跳首次接线漏宿主回调，短音暂停/退出生命周期不完整。 | 当前包有 12 段程序作曲 BGM、22 个短音、三类环境层；34 个 MP3 的来源哈希、解码、响度与主要事件触发已核对，暂停/静音与节点 4 转屏有专项测试。真人听感、跨关混音仍未签收。 |
| 证据可靠性 | 历史 `survivor_seed1/2` 归档曾复制默认 `seedSalt=0` 报告；节点 4 手机截图文件名沿用 `_390x844`，实际 PNG 为 844×390；CPU 降速 1/4/8 倍性能对照仍全触及 60 FPS 上限。 | 额外种子历史误标已公开更正，上一包和当前包重跑真实 seed 1/2，归档脚本校验种子。截图实际尺寸已注明；性能对照只说明本机未观察到回退，不能推断实体设备帧率。见 `evidence/seed_evidence_correction_20260925.md`、`evidence/review_capability_audit_20260925.md`。 |

当前包桌面、手机模拟各 12/12、23 项精确包专项、六项工作树构建门及场景卫生 53/53 通过；`evidence/standalone_bow_action_candidate_evidence_20260924211606.json` 收录 77 件证据。发布决策仍是 `candidate_allowed`、`releaseCertified=false`、`packageVisualReport=null`、`UNVERIFIED_CANDIDATE`。Safari 当前包、实体设备、真人完整路线、真人音频听感和独立视觉审计尚缺。

## 部门 Agent 台：做得对的地方

- 主干与逐关 scope、部门所有权、受控 patch、先草案后人审的设计限制了自动筹备改写原卡、modifier、奖励及战斗数值。当前状态为主干及 12 关共 91 个应确认部门 `confirmed`，13 个导演汇总仍为 `ready_for_review`。
- 82 条交接没有因“部门已确认”全部自动消失；实际仍有 4 条开放：主干音频跨关实听与节点 2/4/5 美术独立视觉审计。精确包发布决策独立于筹备状态，未将候选包误标为 certified release。
- 多次失败与旧包证据保留为轨迹，新的 ZIP 与报告用 SHA-256 绑定；历史种子误标也有明确更正。

## Agent 台需要改进的地方

| 优先级 | 现状证据 | 改进建议 |
| --- | --- | --- |
| P1 · 范围计数/列表错误 | 82 条交接仅 4 条开放（`art` 的节点 2/4/5 各 1，`audio` 的主干 1）；但 `backend/main.py::refresh_open_handoff_counts` 只按 `to` 汇总，导致 26 个已确认的部门 scope 仍有非零徽标，例如所有 art scope 都显示 3。`DepartmentPrepPanel.tsx` 的详情列表也只按 `from/to` 过滤，未按 `unitId` 过滤。 | 以 `(unitId, departmentId)` 计算数量与详情，主干/单关视图只显示本 scope；全关视图另标“全局”。加回归：在节点 2 关闭美术交接后，节点 4/5 的计数各仍为 1，主干美术为 0。 |
| P1 · 交接合同缺结构化验收 | 本工作区 82/82 条交接的 `needs`、`payloadRef`、`blockers` 均为空。自动筹备物化交接时把这些字段写成空数组/空串。至少 `ho_1790137048_a863`（节点 9 尚缺美术覆盖证据）和 `ho_1790137836_57a1`（节点 11 尚缺图集氛围）被以“只要求保持已有字段”的通用说明关闭，与其请求正文不符；后来相关工作另有取证，但原交接的签收轨迹不准确。 | 创建交接时拆出 `acceptanceCriteria`、`evidenceKind`、`candidateSha256`、`ownerScope`；关闭时逐条核对，要求 evidence path/hash 或明确的 `wontfix` 理由。对这两条历史误签收追加更正记录，不静默改写历史。 |
| P1 · 质检分与发布证据混淆 | 质检页自称分数“占位”且可手填；后端确认接口直接接受 `qaScore`。阶段门禁从全局 `capabilities/reports/*_latest.json` 聚合分数，其中 `visual_audit_latest.json` 创建于 2026-07-23，与当前 2026-09-24 ZIP 无精确身份；UI 显示 91 个部门已确认、阶段终点 `runtime_stage`，而发布决策明确 `releaseCertified=false`。 | 将“筹备确认”“当前候选包自动测试”“真人/设备/视觉发布验收”分成不同状态；QA 分数显示来源、生成时间与包 SHA，不符则显示过期，不允许手填分数替代报告。把当前 release decision 的 blockers 以只读方式并列展示在 Agent 台。 |
| P2 · Reject 可被空证据消除 | 当前无开放 reject；但 UI 有批量“将 reject 记为后续优化并关闭”，后端 `/resolve` 未校验 note、证据或目标 status，单条也可直接“标记已解决”。资产确认前 open reject 是硬阻断，关掉后门禁不再见它。 | reject 的 `resolved` 必须附对应修复证据；无法修复则用显式 `waived/deferred` 决策、原因、责任人与影响范围，不能伪装成已解决。删除通用批量关闭捷径。 |
| P2 · Agent 能力呈现与同人策略 | 当前 91 个应确认部门记录中 65 个来自 `procedural_fallback`、26 个来自 LLM，说明大部分是程序草案；美术/音频的聊天入口共用 `code_foundry`。注册表合规职责仍写“de-theme review”和“Block public export on copyright”，与用户确认的个人非商业同人定位不完全一致。 | 卡片常驻显示实际执行源和能力边界，草案/程序回退不等于专业验收。合规提示改为按项目定位核对非官方、免费、来源与具体平台传播风险；技术候选不索要商业 IP 授权材料，也不推定非商业即取得公开传播许可。 |

建议先修 P1 的 scope 计数与结构化签收，再接当前包的发布证据看板；这些是 Agent 台本身的可信度问题。P2 不妨碍继续制作，但在再次大量派工或批量签收前应处理。以上是流程/界面评估，不修改当前 ZIP 或更改四条开放交接的状态。

## 修复记录（2026-09-25）

本次已修正 scope 计数与筛选、未来交接的验收条件和带哈希证据签收、`reject` 的关闭限制、只读筹备分、报告来源与候选包身份显示、批量关闭入口及同人合规文案。旧交接的空字段和两条不准确的历史签收说明保持原貌，避免伪造过去的验收过程；相应问题已在上表留痕。当前四条开放交接仍需各自的真实审计或实听证据。本次修复没有改动候选 ZIP，发布决策仍以独立发布门禁为准。
